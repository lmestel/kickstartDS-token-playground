{
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "app",
    "worker"
  ],
  "scripts": {
    "build": "yarn workspace style-dictionary-worker run build && yarn workspace app run build",
    "deploy": "netlify deploy --prod --site 9b512689-5496-40cb-a6bf-63548ed1ac62",
    "prepare": "patch-package"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "2.8.3",
    "buffer": "^5.5.0",
    "netlify-cli": "^12.10.0",
    "patch-package": "^6.5.1",
    "punycode": "^1.4.1",
    "querystring-es3": "^0.2.1",
    "url": "^0.11.0"
  }
}
